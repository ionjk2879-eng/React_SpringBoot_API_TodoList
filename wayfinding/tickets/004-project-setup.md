# 모노레포 초기 세팅
label: wayfinder:task
status: closed

## Question

모노레포 구조를 잡고 Spring Boot 3 + Vite React 프로젝트를 초기화한다.

완료 기준:
- `backend/` — Spring Boot 3 + JPA + H2 + Spring Security 의존성 포함
- `frontend/` — Vite + React + TypeScript + Tailwind + React Query 설치 완료
- 로컬에서 각각 `./gradlew bootRun`, `npm run dev` 실행 가능

Blocks: 001, 002, 003 결정 이후 구현 시작
