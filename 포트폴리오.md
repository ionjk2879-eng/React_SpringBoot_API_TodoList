# Todo List — 포트폴리오

> **풀스택 할 일 관리 웹 애플리케이션**  
> 서비스 주소: http://13.125.211.216/  
> GitHub: https://github.com/ionjk2879-eng/React_SpringBoot_API_TodoList

---

## 프로젝트 개요

카테고리 분류, 반복 일정, 마감 추적, 달력 뷰, 커스텀 도장 이미지까지 지원하는  
풀스택 할 일 관리 서비스입니다. 기획부터 배포·운영까지 단독으로 진행했습니다.

- **개발 기간:** 2025년 (약 2개월)
- **역할:** 기획 · 디자인 · 프론트엔드 · 백엔드 · 인프라 전담

---

## 기술 스택

### Frontend
- **React 18** + **TypeScript** — 컴포넌트 기반 UI
- **Vite** — 빠른 HMR 및 프로덕션 번들
- **Tailwind CSS v4** — 유틸리티 퍼스트 스타일링
- **TanStack React Query v5** — 서버 상태 캐싱 · 뮤테이션 관리
- **Axios** — HTTP 클라이언트, JWT 인터셉터 구현

### Backend
- **Java 21** + **Spring Boot 3**
- **Spring Security** + **JWT (jjwt 0.12.6)** — 인증/인가
- **Spring Data JPA** + **Hibernate** — ORM
- **MySQL 8** (프로덕션) / **H2** (로컬 개발)
- **Gradle (Kotlin DSL)** — 빌드

### Infrastructure
- **AWS EC2** — 애플리케이션 서버
- **AWS RDS MySQL 8** — 데이터베이스
- **GitHub Actions** — CI/CD 파이프라인 (push to main → 자동 배포)
- **systemd** — 프로세스 관리

---

## 구현 기능

### 핵심 도메인
| 기능 | 세부 내용 |
|---|---|
| 할 일 CRUD | 제목, 내용, 마감일, 카테고리, 반복 설정 |
| 소프트 딜리트 | 삭제 시 휴지통 이동, 7일 후 자동 영구 삭제 (스케줄러) |
| 반복 일정 | 매일/매주 반복, 종료일 설정, 완료 시 다음 항목 자동 생성 |
| 마감 상태 분류 | 해야 할 일 / 마감임박(24h 이내) / 기한초과 / 완료 — 자동 분류 |
| 하위 할 일 | 서브태스크 단위 관리 |
| 카테고리 | 색상 · 커스텀 도장 · 고정 · 보관 · 드래그 순서 변경 |

### 인증
- JWT Access Token + Refresh Token 이중 토큰 구조
- Axios 응답 인터셉터에서 401 수신 시 자동 갱신 후 원래 요청 재시도
- Refresh Token 사용 시 쿠키 Rotation (재발급)

### UI/UX
- **달력 뷰** — 월별 마감 할 일 시각화, 날짜 클릭 필터, 드래그 이동
- **커스텀 도장** — 카테고리별 이미지 업로드 + 브라우저 내 크롭 (`canvas` API)
- **프로필 아바타** — 이미지 업로드 · 크롭
- **포인트 컬러** — 7가지 테마 색상, CSS 변수 런타임 교체
- **키보드 단축키** — `N` 새 할 일 / `Esc` 닫기 / `Space` 완료 토글 / `E` 수정
- **모바일 반응형** — 사이드바 드로어 오버레이, 스택 레이아웃
- **브라우저 Push 알림** — 마감 전 알림 (Notification API)

---

## 기술적 도전 및 해결

### 1. JWT 401 무한 루프 방지

**문제:** Access Token 만료 시 갱신 요청 자체도 401이 날 경우, 인터셉터가 무한 재귀 호출되는 문제.

**해결:**
```typescript
// Axios 인터셉터 내부
if (error.response?.status === 401 && !original._retry) {
  original._retry = true;   // 재시도 플래그로 루프 차단
  if (isRefreshing) {
    // 갱신 중이면 큐에 적재 후 토큰 받으면 일괄 재시도
    return new Promise((resolve, reject) => {
      failedQueue.push({ resolve, reject });
    });
  }
  // ... 갱신 후 큐 flush
}
```
갱신 중 추가 요청은 `failedQueue`에 쌓아두고, 토큰 갱신 완료 후 일괄 재시도하는 방식으로 중복 갱신과 무한루프를 동시에 방지했습니다.

---

### 2. 반복 일정 중복 생성 버그

**문제:** 반복 일정을 완료 → 해제 → 재완료할 때마다 다음 반복 항목이 중복 생성되는 버그.

**원인:** `toggleComplete` 서비스 로직이 `completing == true` 조건만 체크하고, 이미 다음 항목이 생성됐는지 추적하지 않음.

**해결:** `Todo` 엔티티에 `nextSpawned` boolean 컬럼을 추가해 최초 완료 시 1회만 스폰하도록 제어:
```java
boolean shouldSpawn = completing
    && todo.getRecurrence() != null
    && todo.getDeadline() != null
    && !todo.isNextSpawned();   // 이미 생성됐으면 스킵

if (shouldSpawn) {
    todo.setNextSpawned(true);
}
todoRepository.save(todo);

if (shouldSpawn) {
    // 다음 반복 항목 생성
}
```

---

### 3. 포인트 컬러와 상태 색상 충돌

**문제:** 포인트 컬러 테마 변경 시 CSS 변수 `--color-orange-*`를 런타임에 덮어쓰는 방식을 사용했는데, "마감임박" 상태 카드가 `orange-*` Tailwind 클래스를 쓰고 있어 테마 색상으로 오염되는 문제.

**해결:** Tailwind v4에서 `amber-*` 클래스는 `--color-amber-*` 변수를 참조하므로 테마 override의 영향을 받지 않는 점을 이용해, 마감임박 상태 색상을 `orange-*` → `amber-*`로 교체. 기능적 색상과 브랜드 컬러를 완전히 분리:
```js
// 테마: --color-orange-* 변수만 교체
// 상태 색상: amber-* (별도 CSS 변수, 테마 영향 없음)
{ key: 'progress', color: 'text-amber-600 border-amber-200 bg-amber-50' }
```

---

### 4. Flex 레이아웃 텍스트 줄바꿈

**문제:** 패널 너비가 줄어들 때 제목이 단어 단위가 아니라 글자 단위로 줄바꿈 되는 현상.

**원인:** Flex 아이템의 기본 `min-width: auto`가 컨테이너 축소를 막아 텍스트가 강제로 깨지는 현상.

**해결:**
```jsx
<div className="min-w-0 flex-1">         {/* min-w-0으로 flex shrink 허용 */}
  <h1 className="truncate">제목</h1>    {/* overflow 시 말줄임 */}
</div>
<div className="flex-shrink-0">         {/* 버튼은 고정 크기 유지 */}
  <button>새 할 일</button>
</div>
```

---

### 5. SSH Broken Pipe (CI/CD)

**문제:** GitHub Actions에서 EC2에 SSH로 접속 후 `npm run build`(약 2~3분 소요) 중 연결이 끊기며 `exit 255` 오류.

**해결:** SSH 옵션에 keep-alive 설정 추가:
```yaml
ssh -i ~/.ssh/deploy.pem \
  -o ServerAliveInterval=60 \
  -o ServerAliveCountMax=10 \
  ...
```

---

### 6. Stale Closure 문제 (키보드 단축키)

**문제:** `useEffect` 안에서 등록한 `keydown` 핸들러가 최신 React 상태를 읽지 못하는 stale closure 문제.

**해결:** `useRef`로 현재 상태의 스냅샷을 유지하고, 렌더링마다 ref를 업데이트. 이벤트 핸들러는 ref를 통해 항상 최신 상태를 참조:
```typescript
const shortcutRef = useRef({ showForm, editingTodo, focusedTodoId, ... });

useEffect(() => {
  shortcutRef.current = { showForm, editingTodo, focusedTodoId, ... };
}); // 매 렌더 업데이트

useEffect(() => {
  const handler = (e) => {
    const s = shortcutRef.current; // 항상 최신 상태
    // ...
  };
  document.addEventListener('keydown', handler);
  return () => document.removeEventListener('keydown', handler);
}, []); // 단 1회 등록
```

---

## 프로젝트 구조

```
React_Spring_API/
├── backend/                          # Spring Boot 3 (Java 21)
│   └── src/main/java/com/mysite/todo/
│       ├── domain/
│       │   ├── todo/                 # 할 일 (CRUD, 반복, 토글)
│       │   ├── category/             # 카테고리 (정렬, 도장 이미지)
│       │   ├── subtask/              # 하위 할 일
│       │   └── user/                 # 사용자 (프로필, 설정)
│       └── global/                   # JWT, Security, CORS, 예외처리
│
├── frontend/                         # React 18 + TypeScript
│   └── src/
│       ├── pages/TodoPage.tsx        # 메인 화면 (1,700+ lines)
│       ├── components/               # TodoCard, Calendar, TodoForm 등
│       ├── api/                      # Axios 클라이언트 + 인터셉터
│       ├── hooks/                    # 마감 알림 커스텀 훅
│       └── utils/                    # 포인트 컬러, 카테고리 색상
│
└── .github/workflows/deploy.yml      # GitHub Actions CI/CD
```

- 백엔드 Java 파일: **42개**
- 프론트엔드 소스 파일: **30개+**
- 전체 커밋: **35개+**

---

## CI/CD 파이프라인

```
git push (main)
    ↓
GitHub Actions
    ├── Gradle bootJar 빌드
    ├── SCP → EC2에 JAR 전송
    └── SSH → git pull + npm build + systemctl restart
```

코드 푸시 한 번으로 백엔드(JAR)와 프론트엔드(Vite 빌드) 모두 자동 배포됩니다.

---

## 화면 구성

| 영역 | 설명 |
|---|---|
| 사이드바 | 카테고리 목록 / 설정 전환, 드래그 정렬, 고정·보관 |
| 할 일 목록 | 날짜별 그룹, 상태 카드 필터, 실시간 검색 |
| 달력 | 마감 기준 월별 시각화, 드래그로 마감일 변경 |
| 설정 | 프로필 이미지, 닉네임, 포인트 컬러, 알림, 자동 정리 |
| 모바일 | 사이드바 → 드로어 오버레이, 2열 상태 카드 |
